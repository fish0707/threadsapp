# Threads `keyword_search` App Review 送審清單

> 目的：讓 App 通過 Meta 審核，從「只搜得到自己貼文」解鎖成「可搜公開熱門貼文」。
> 對象權限：`threads_keyword_search`（`threads_basic` 通常隨基本流程即可，`threads_content_publish` 另外送審）。

---

## 0. 送審前先確認（缺一項就會被退）

- [ ] App 已可正常運作（OAuth 登入、搜尋、生成、發佈都能跑）。
- [ ] **隱私政策網址**（公開可連、內容對得上你 App 的資料行為）。
- [ ] **資料刪除說明 / 回呼**（我們已做 `/api/auth/threads/deletion`）。
- [ ] App 圖示（1024×1024）、顯示名稱、用途分類填好。
- [ ] 有一個**可被 Meta 審核員實際操作或觀看**的環境（正式網址或螢幕錄影）。
- [ ] 商業驗證（Business Verification）— 若被要求，需準備公司／個人證明文件。自用測試階段不一定觸發，但搜公開貼文常會要求。

---

## ★ 後台「應用程式審查要求」精靈逐欄填寫

審查精靈共 5 個分頁：**驗證 → 應用程式設定 → 允許的使用方式 → 資料處理 → 審查人員指示**。
（右上角會「已自動儲存」。）以下逐欄給可直接複製的內容。
> 文中網域請換成你的：`https://threadsapp-smoky.vercel.app`

### 分頁 1：驗證
- 若出現「商業驗證 / 身分驗證」要求 → 依指示上傳文件（個人可走個人身分驗證）。
- 自用測試階段有時不會卡這關；若搜公開貼文被要求驗證，就把文件補齊。
- 沒有要求就直接「繼續」。

### 分頁 2：應用程式設定（必填才能送審）

| 欄位 | 怎麼填 |
|------|--------|
| **應用程式圖示** | 上傳 512×512～1024×1024 的 JPG/PNG（≤5MB）。沒有的話用 repo 附的 `docs/app-icon.png`（1024×1024）。點「編輯應用程式圖示」上傳。 |
| **隱私政策網址** | `https://threadsapp-smoky.vercel.app/privacy` ← 先用瀏覽器確認打得開、非 404、非地理封鎖。 |
| **應用程式類別** | 下拉選 **Productivity（生產力）**（找不到就選 **Business / 商業與頁面**）。 |

> 隱私政策那欄下方可用「Developer URL Debugger」測你的網址，確認 Meta 爬蟲抓得到。

### 分頁 3：允許的使用方式（逐個權限的用途說明）

針對每個要送審的權限各貼一段。**重點：講清楚「使用者本人用、僅參考顯示、不轉售、非自動轟炸」。**

**`threads_basic`**
```
本應用程式使用 threads_basic 取得已授權使用者本人的基本 Threads 帳號資訊
（user id、username），用於識別使用者身分並代其在自己的帳號上發佈內容。
```

**`threads_content_publish`**
```
使用者在本應用程式撰寫並確認貼文草稿後，點擊「發佈到 Threads」即會將該則
貼文發佈到「使用者本人」的 Threads 帳號。我們僅在使用者每一次明確點擊發佈時
才呼叫此權限，不進行自動排程或批次大量發文，發佈前並提供預覽以避免誤發。
```

**`threads_keyword_search`**
```
本應用程式「脈脈 Threads 經營助手」協助使用者經營自己的 Threads 帳號。
使用者輸入一個主題關鍵字後，我們呼叫 keyword_search（search_type=TOP）取得
該主題當天的公開熱門貼文，於「靈感動態」清單中作為使用者創作時的參考顯示。

使用此權限的唯一目的，是讓使用者了解某主題目前的熱門內容趨勢，據以撰寫
自己的原創貼文。我們不抓取、不儲存散布、不轉售、也不重新發佈他人貼文內容，
僅在使用者本人創作期間作為畫面參考。資料不用於廣告或建立使用者輪廓。
```

### 分頁 4：資料處理

說明你如何處理資料（可直接貼，並附隱私政策連結）：
```
我們蒐集：使用者授權的 Threads 存取權杖、user id、username；使用者建立的主題、
AI 生成草稿與發佈紀錄。存取權杖以 AES-256-GCM 加密後儲存於 Supabase，
不出現在前端或網址。透過 keyword_search 取得的公開貼文僅作畫面參考顯示，不另作他用。

第三方處理者：Meta（Threads API）、Vercel（代管）、Supabase（資料庫）、
Google Gemini / Anthropic Claude（生成草稿）。

刪除機制：使用者可於 App 內「解除連結」、於 Threads 移除授權（我們以
deauthorize / data deletion 回呼刪除權杖），或來信要求刪除全部資料。
隱私政策：https://threadsapp-smoky.vercel.app/privacy
資料刪除說明：https://threadsapp-smoky.vercel.app/data-deletion
```
- 若有「資料刪除回呼網址」欄位 → `https://threadsapp-smoky.vercel.app/api/auth/threads/deletion`

### 分頁 5：審查人員指示（最關鍵，決定過不過）

附上**測試步驟 + 測試帳號 + 錄影**。可直接貼：
```
測試網址：https://threadsapp-smoky.vercel.app

測試步驟：
1. 開啟測試網址，點右上「連結 Threads」，使用 Threads 帳號完成 OAuth 授權
   （請授權 threads_basic、threads_content_publish、threads_keyword_search）。
   授權後右上會顯示已連結的帳號。
2. 在首頁搜尋框輸入主題關鍵字（例如「職場」），按「搜尋熱門」。
3. 左側「靈感動態」會列出該主題的公開熱門貼文 —— 此即 keyword_search 的使用。
4. 勾選 1～2 篇作為參考，於右側選擇風格後按「生成草稿」，會產生原創貼文草稿。
5. 點「發佈到 Threads」可將草稿發佈到已連結帳號（content_publish 的使用）。

附件影片已示範上述完整流程（輸入關鍵字→出現搜尋結果→生成→發佈）。
備註：本權限尚未核准前，keyword_search 僅回傳授權帳號本人的貼文，核准後即可
回傳公開熱門貼文，功能與畫面相同。
```
- 上傳**螢幕錄影**（1～2 分鐘，依「3. 操作流程」腳本）。**務必拍到「輸入關鍵字 → 出現結果」**。
- 填好按「繼續 / 提交」。

### 最後：把 App 切換為 Live（上線）
左側「**發佈**」目前是「尚未發佈」。基本資訊齊全後，把開發模式切到 **Live**，
審查與正式存取才會生效。



需要一頁公開網頁，至少說明：
- 你會透過 Threads API 取得哪些資料（公開貼文內容、作者公開資訊、你授權帳號的 user id / token）。
- 用途（產生貼文草稿、發佈貼文）。
- 儲存方式與期間（token 加密儲存於資料庫；草稿儲存於資料庫）。
- 使用者如何要求刪除資料（提供聯絡方式或刪除流程）。
- 第三方服務揭露（Vercel、Supabase、Google Gemini／Anthropic）。

> 可放在 `https://你的網域/privacy`。要的話我可以幫你產一頁隱私政策範本頁面。

---

## 2. 權限用途說明（Permission rationale）

審核表單會要你針對 `threads_keyword_search` 說明「為什麼需要、怎麼用」。範本（可直接改）：

```
我們的應用程式「脈脈 Threads 經營助手」協助使用者經營自己的 Threads 帳號。
使用者輸入一個主題關鍵字後，我們呼叫 keyword_search（search_type=TOP）取得
該主題當天的公開熱門貼文，作為「靈感參考」呈現給使用者瀏覽。

使用 threads_keyword_search 的唯一目的，是讓使用者了解某主題目前的熱門內容趨勢，
並據以撰寫自己的原創貼文。我們不會抓取、轉售或重新發佈他人貼文內容，
僅作為使用者本人創作時的參考顯示。資料不會用於廣告或建立使用者輪廓。
```

要點：
- 明確說「**作為參考顯示**」，不是抓資料轉售／重新發佈。
- 對應到畫面上的「靈感動態」功能。
- 不要寫成「自動大量爬取」或「監控他人」——會被退。

---

## 3. 操作流程（Step-by-step / 螢幕錄影腳本）

審核員需要能重現功能。準備一段 1～2 分鐘錄影，照這個腳本：

1. 開啟 App 首頁，點「連結 Threads 帳號」，完成 OAuth 授權（顯示授權畫面 → 回到 App 顯示已連結）。
2. 在搜尋框輸入主題（例：`職場`），按「搜尋熱門」。
3. 畫面出現「靈感動態」公開熱門貼文卡片（**這一步就是在展示 keyword_search**）。
4. 勾選 1～2 篇參考 → 選風格 → 按「生成草稿」→ 出現 AI 生成的原創草稿。
5. （可選）按「發佈到 Threads」展示發佈流程。

> 錄影需清楚拍到「輸入關鍵字 → 出現搜尋結果」的因果，審核員才看得出你真的用到這個權限。

---

## 4. 測試帳號 / 測試說明

- 提供審核員可用的**測試 Threads 帳號**（或說明用他們自己的帳號授權即可）。
- 在「App Review 說明欄」附上：正式網址、測試步驟、預期結果、上面那段錄影連結。

---

## 5. 送審操作位置

Meta 開發者後台 → 你的 App →
1. **使用案例（Use cases）→ 存取 Threads API → 權限和功能**：把 `threads_keyword_search` 旁的狀態從「測試中」推進到「要求進階存取（Request Advanced Access）」。
2. 依表單填寫：用途說明（第 2 節）、操作步驟（第 3 節）、上傳／貼上錄影。
3. **App 從開發模式切換為上線（Live）** 才會正式生效（基本資訊、隱私政策、圖示需齊全才能切上線）。
4. 送出後等待審核（通常數個工作天，可能來回補件）。

---

## 6. 常見被退原因 & 對策

| 被退原因 | 對策 |
|----------|------|
| 隱私政策無法連、或內容與功能不符 | 用第 1 節清單補齊，網址需公開可連 |
| 錄影看不出用到該權限 | 照第 3 節腳本，明確拍「輸入關鍵字→出現結果」 |
| 用途寫得像爬蟲／轉售他人內容 | 強調「使用者本人創作時的參考顯示」 |
| App 還在開發模式 | 切換為 Live（補齊基本資訊） |
| 缺資料刪除機制 | 已內建 `/api/auth/threads/deletion`，於後台填入 |
| 未完成商業／身分驗證 | 依 Meta 要求上傳文件 |

---

## 7. 審核通過後

- 不需改任何程式：本專案的 `searchTopic()` 已直接呼叫 `keyword_search`，
  通過後同一支 API 就會回傳**公開**熱門貼文（而非只有自己的）。
- 注意額度：每使用者滾動 24 小時最多 2,200 次查詢（跨 app 共用）。
  本專案已用「同主題當天快取」節流，正常使用不易觸頂。

---

### 還沒過審期間的替代方案
若想在審核通過前就有「公開熱門文」可參考，可評估第三方資料源
（Apify / EnsembleData 等），抽象化在 `src/lib/threads.ts` 的資料來源層即可替換，
但有成本與合規考量（見計畫書風險表）。
